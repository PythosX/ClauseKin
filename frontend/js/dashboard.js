async function load(){shell('dashboard','Good morning 👋','Your contract obligations at a glance.');let c=await api('/contracts'),o=await api('/obligations');document.querySelector('#content').innerHTML=`<div class="card hero"><h2 class="gradient-text">Turn contracts into actions.</h2><p class="muted">Upload a business agreement and extract key terms, obligations, deadlines and source references.</p><a class="btn" href="/pages/contracts.html">Open contracts</a> <a class="btn secondary" href="/pages/chat.html">Ask a question</a></div><div class="grid stats" style="margin-top:16px"><div class="card"><div class="label">Contracts</div><div class="value">${c.length}</div></div><div class="card"><div class="label">Obligations</div><div class="value">${o.length}</div></div><div class="card"><div class="label">High risk</div><div class="value">${o.filter(x=>x.risk==='High').length}</div></div><div class="card"><div class="label">Due soon</div><div class="value">${o.filter(x=>new Date(x.deadline)-Date.now()<30*86400000).length}</div></div></div><div class="grid two" style="margin-top:16px"><div class="card"><h3>Upcoming obligations</h3><table class="table"><tr><th>Obligation</th><th>Deadline</th><th>Risk</th></tr>${o.slice(0,8).map(x=>`<tr><td>${esc(x.description)}<div class="kpi">${esc(x.contract_name)}</div></td><td>${fmt(x.deadline)}</td><td><span class="badge ${x.risk.toLowerCase()}">${x.risk}</span></td></tr>`).join('')||'<tr><td colspan=3>No obligations yet.</td></tr>'}</table></div><div class="card"><h3>Human review</h3><div class="notice">AI output is a review aid. Verify important terms against the source contract.</div><p class="muted">Every extracted obligation keeps a section and page reference.</p></div></div>`}load().catch(console.error)
