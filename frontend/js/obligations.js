async function load(){shell('obligations','Obligation timeline','Turn contractual duties into an actionable schedule.');let o=await api('/obligations');document.querySelector('#content').innerHTML=`<div class="card"><div class="timeline">${o.map(x=>`<div class="event"><h4>${esc(x.description)}</h4><div class="muted">${fmt(x.deadline)} · ${esc(x.contract_name)}</div><p>${esc(x.party)} · ${esc(x.frequency)} · <span class="badge ${x.risk.toLowerCase()}">${x.risk}</span></p><div class="kpi">Source §${esc(x.section)} · page ${x.page}</div></div>`).join('')||'<p class="muted">Upload a contract to create obligations.</p>'}</div></div>`}load()
