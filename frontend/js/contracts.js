async function load(){shell('contracts','Contracts','Upload, inspect and manage agreements.');document.querySelector('#content').innerHTML=`<div class="card"><div class="drop"><h3>Upload a contract</h3><p class="muted">PDF, DOCX or TXT</p><input id="file" type="file" accept=".pdf,.docx,.txt"><button class="btn" id="upload">Analyze contract</button></div></div><div id="list" class="cards" style="margin-top:16px"></div>`;async function render(){let c=await api('/contracts');document.querySelector('#list').innerHTML=c.map(x=>`<div class="card contract-card" onclick="location='/pages/contract.html?id=${x.id}'"><div class="kpi">ACTIVE CONTRACT</div><h3>${esc(x.name)}</h3><p class="muted">${esc(x.filename)}</p><span class="badge low">${esc(x.status||'Analyzed')}</span><span class="kpi"> ${fmt(x.expires)} expiry</span></div>`).join('')||'<div class="card"><p class="muted">No contracts yet.</p></div>'}document.querySelector('#upload').onclick=async()=>{let f=document.querySelector('#file').files[0];if(!f)return alert('Choose a file first');let fd=new FormData();fd.append('file',f);let r=await fetch('/api/contracts/upload',{method:'POST',body:fd});if(!r.ok)return alert(await r.text());await render();alert('Contract analyzed successfully')};render()}load()
