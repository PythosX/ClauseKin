async function load(){shell('alerts','Alerts','Upcoming contractual actions.');let o=await api('/obligations');let a=o.filter(x=>new Date(x.deadline)-Date.now()<30*86400000);document.querySelector('#content').innerHTML=`<div class="cards">${a.map(x=>`<div class="card"><span class="badge ${x.risk.toLowerCase()}">${x.risk}</span><span class="kpi"> ${fmt(x.deadline)}</span><h3>${esc(x.description)}</h3><p class="muted">${esc(x.contract_name)}</p><div class="source">Responsible: ${esc(x.party)}<br>Source §${esc(x.section)} · page ${x.page}</div></div>`).join('')||'<div class="card"><p class="muted">No upcoming alerts.</p></div>'}</div>`}load()
