async function load(){shell('chat','Ask ContractLens','Ask questions grounded in indexed contract clauses.');let c=await api('/contracts');document.querySelector('#content').innerHTML=`<div class="grid two"><div class="card"><h3>Ask a question</h3><select id="contract"><option value="">All contracts</option>${c.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select><textarea id="q" placeholder="What happens if we terminate the agreement?"></textarea><button class="btn" id="ask">Ask AI</button></div><div class="card"><h3>Answer</h3><div id="answer" class="muted">Your answer will appear here.</div></div></div>`;document.querySelector('#ask').onclick=async()=>{let q=document.querySelector('#q').value.trim();if(!q)return;document.querySelector('#answer').textContent='Thinking…';let r=await api('/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q,contract_id:document.querySelector('#contract').value?Number(document.querySelector('#contract').value):null})});document.querySelector('#answer').innerHTML='<div class="source">'+esc(r.answer).replace(/\n/g,'<br>')+'</div>'}}load()
